currency:
  symbol: "$" # short symbol to identify the currency
  name: "Dollar" # long name
  starting_balance: 500 # starting balance every new player gets

database:
  db_path: "data/database.db" # relative from the server run directory
  cache_sync: 1 # defines after how many MINUTES the cache will sync to disk